1、流程图全部使用Service Task，可以模拟自动执行的场景，我们只需要启动一个流程实例，它会自动执行到结束。
    这种情况下，不需要关注流程的执行进度，而只需要把精力集中在每个结点的处理逻辑（通常是简单或者复杂的商业逻辑）上。

    Each node of the process diagram USES a Service Task that simulates an automated execution scenario.
    We only need to start a process instance that will execute automatically to the end.
    In this case, you don't need to focus on the execution of the process, but instead focus on
    the processing logic of each node (usually simple or complex business logic).

2、